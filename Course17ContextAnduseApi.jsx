// I)Context:
//   Context lets you share data across your component tree without having to manually pass
//   props through every intermediatecomponent:
//   It lets you:
//     -Lets you broadcast a value at high level.
//     -Any component nested inside can subscribe to it directly.

// II)use API:
//    -use is simply a more flexible alternative to useContext.
//    -It does the same job (reading values from context) but without the restriction that come with hooks.
